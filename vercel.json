{
  "buildCommand": "pip install -r requirements.txt && playwright install chromium",
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "memory": 3008,
      "maxDuration": 60
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "app.py"
    }
  ]
}
